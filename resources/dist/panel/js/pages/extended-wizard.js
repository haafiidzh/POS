/**
 * Author: SOC Software House
 * Module/App: extended-wizard.js
 */

import "@glhd/alpine-wizard";

// window.initWizard = (el, config) => {
//     const element = document.querySelector(`#${el}`);
//     return new Wizard(element, config);
// };
